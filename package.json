{
  "name": "water-lab",
  "version": "0.5.0",
  "private": true,
  "type": "module",
  "description": "Local-first 3D model and water experiment. Static GitHub Pages-ready prototype.",
  "scripts": { "start": "node server.mjs", "test": "node --import ./tests/register.mjs --test tests/*.test.mjs" }
}
